# CLAUDE.md

## 프로젝트 최상위 운영 계약

이 저장소의 현재 작업 범위는 다음 세 가지뿐이다.

1. 세계관
2. 설정집
3. 이야기 설계도

실제 소설 원고, 대사, 장면 본문, 회차 원고는 작성하지 않는다.

## 자동 진행 규칙

- 사용자가 `이어서 진행`, `자동으로 진행`, `끝까지 진행`이라고 말하면 세계관·설정집·설계도 작업을 중간 확인 없이 계속한다.
- 후보 설계는 4안을 비교하고, 추천안을 제시하되 사용자의 명시 승인 전에는 최종 정본으로 승격하지 않는다.
- 기존 오염 브랜치의 파일을 자동 신뢰하거나 통째로 이관하지 않는다.
- 다른 소설 프로젝트의 인물명, 작품 수, 화수, 장 수, 사건, 복선, 결말, 플랫폼 구조를 절대 가져오지 않는다.
- `200화`, `7부제`, `117장`, `234장면`, 기존 인물명과 기존 장기 맥거핀은 출처가 확인되기 전까지 격리한다.

## 컨텍스트 종료 전 인수인계 규칙

대화 컨텍스트가 부족해지기 전에 반드시 다음을 수행한다.

1. 현재 작업을 GitHub 브랜치에 커밋하여 원격에 보존한다.
2. 최신 브랜치명과 커밋 SHA를 확인한다.
3. 완료한 작업, 미완료 작업, 다음 작업 순서를 인수인계 문서에 갱신한다.
4. 새 대화창에 그대로 붙여 넣을 연속 작업 프롬프트를 작성한다.
5. 사용자에게 해당 프롬프트를 제공한다.

GitHub contents API로 작성한 모든 변경은 즉시 원격 브랜치에 커밋된 것으로 취급한다.

## 현재 작업 브랜치

`canon/world-bible-provenance-clean`

이 브랜치는 `main`에서 새로 만들었으며 기존 오염 브랜치를 복사하지 않았다.

## 승인·상태 원칙

- 사용자 직접 확인: `P0`
- 직접 확인된 의도에서 도출한 독립 후보: `P1`
- 조사·감수 필요 후보: `P2`
- 출처 불명 또는 오염 의심: `Q`
- 다른 프로젝트 유입 확인: `X`

`P1`과 `P2`는 자동으로 `canon`, `final`, `author-approved`, `complete`로 승격하지 않는다.

## 금지 사항

- 원고 작성
- 기존 원고에서 설정을 역산하여 확정
- 분량을 맞추기 위한 사건 증식
- 다른 작품의 숫자와 구조 재사용
- 사용자 승인 없는 PR 병합 또는 Ready 전환
